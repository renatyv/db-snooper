# Ideas
Where to get the data from for schema linking

* generated .sql profile
* grep through the source code of athe application using the database;
* LLM-summariazation: A short summary (minimal profile) identifies the meaning and format of a field
* declared PK/FK constraints;
* Locality Sensitive Hash (LSH) `https://pypi.org/project/kshingle/0.1.0/` index on shingles `https://pypi.org/project/kshingle/0.1.0/`.
